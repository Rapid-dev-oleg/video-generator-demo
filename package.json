{
  "name": "video-generator-demo",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "install:all": "npm --prefix client install && npm --prefix server install",
    "build": "npm --prefix client run build",
    "start": "node --env-file=server/.env server/server.js",
    "tunnel": "node --env-file=server/.env server/tunnel.mjs",
    "demo": "npm run build && concurrently -k -n server,tunnel \"npm run start\" \"npm run tunnel\""
  },
  "devDependencies": {
    "concurrently": "^9.2.4"
  }
}
